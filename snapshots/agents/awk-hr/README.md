# awk-hr

awk-hr 是数字员工（Agent）生命周期管理器，负责：
1. 创建 Agent（职责、风格、边界）
2. 创建独立 workspace 与标准 md 文件
3. 绑定到指定群组（由用户在创建时提供）
4. 删除 Agent 及其全部数据（强制二次确认）

## 删除安全机制
- 第一步：提交删除申请，生成任务号
- 第二步：用户输入“确认删除 <任务号>”后才执行永久删除
